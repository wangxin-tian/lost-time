/*
  视口：
    viewport:
    布局视口 == 视觉视口
    <meta name="viewport" 
      content="initial-scale=1,width=device-width" />
    
    initial-scale=1 初始缩放比是1
    user-scalable=yes 允许缩放
    user-scalable=no
    maximun-scale=5
    minimum-scale=0.5

    禁止缩放
    user-scalable=no
    maximun-scale=1
    minimum-scale=1
 */

/*
  JS获取视口宽度
  document.documentElement.clientWidth
  document.documentElement.getBoudingClientRect().width
*/

