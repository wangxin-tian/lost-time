/*

    object
      key to type
      
    interface
      对对象加以限制
      接口相当于输出， 所以接口的属性都是公有的

    高内聚、低耦合
      功能相关的事物应该形成一个模块
      模块之间应该减少耦合

    constructor中使用访问修饰符后， 会自动生成模板代码

    <K, T = number>(x: K, y: T)
 */